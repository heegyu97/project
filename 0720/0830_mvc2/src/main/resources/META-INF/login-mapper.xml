<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="LoginMapper">

	<select id="login" parameterType="kr.co.ictedu.util.dto.MemberDTO"
						resultType="kr.co.ictedu.util.dto.MemberDTO">
			select
					m_no, m_id, m_pwd, m_email
			from member
			where m_id = #{m_id}
			and m_pwd = #{m_pwd}
		
	</select>
	
	<select id="idCheck" parameterType="java.lang.String"
						resultType="kr.co.ictedu.util.dto.MemberDTO">
			select m_no, m_id, m_pwd, m_email
			from member
			where m_email = #{m_email}
		
	</select>
	
	
	<select id="idCheck2" parameterType="java.lang.String" resultType="int">
		select count(m_id) cnt from member
		where m_id = #{m_id}
	</select>
	
	
	<insert id="join" parameterType="kr.co.ictedu.util.dto.MemberDTO">
			
		INSERT INTO member(m_id,m_pwd,m_email)
		VALUES(#{m_id},#{m_pwd},#{m_email})
	</insert>
</mapper>
