<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SpaceMapper">
	
	
	<select id="list" resultType="kr.co.ictedu.util.dto.SpaceDTO">
		select w_no, w_title, w_cnt, w_link, r_no, m_no 
		from cntwrite
	
	</select>
	
	<insert id="insert" parameterType="kr.co.ictedu.util.dto.RoomDTO">
		insert into room 
		( r_title, r_cdate, r_op1, r_op2) 
		values 
		(#{r_title},#{r_cdate},#{r_op1},#{r_op2} ) 
	
	</insert>


</mapper>
