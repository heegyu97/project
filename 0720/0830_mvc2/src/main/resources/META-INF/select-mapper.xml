<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SelectMapper">
	
	<select id="list2" parameterType="kr.co.ictedu.util.dto.RoomDTO" resultType="kr.co.ictedu.util.dto.RoomDTO">
			select r_no, r_title, r_sdate, r_cdate , r_op1, r_op2, r_op3, m_no
			from croom
			where m_no = #{m_no} 
	</select>
	
	
	<select id="list" parameterType="kr.co.ictedu.util.dto.RoomDTO" resultType="kr.co.ictedu.util.dto.RoomDTO">
			select r_no, r_title, r_sdate, r_cdate , r_op1, r_op2, r_op3, m_no, r_back_img, r_list_img
			from room
			where m_no = #{m_no} 
			order by r_cdate
			limit #{limitNum}, 3 
			
	</select>


	<select id="searchListCount" parameterType="kr.co.ictedu.util.dto.RoomDTO" resultType="int">
			select count(r_no) cnt from room
			where m_no = #{m_no}
	</select>
</mapper>

